// Objetivo: detectar redeclaração de variável 'variavel_decimal' em um escopo interno aninhado.
// Esperado: ERR_DECLARED na linha 'var variavel_decimal := decimal' dentro do bloco 'senao', pois já existe uma variável com o mesmo nome em um escopo acima.

principal -> inteiro :=
[
    enquanto (1)
    [
        var variavel_decimal := decimal

        se (1)
        [
            se (0)
            [
            ]
            senao
            [
                var variavel_decimal := decimal
            ]
        ]
    ]

    retorna 0 := inteiro
];
